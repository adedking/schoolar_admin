import React from 'react';

const ParentMessages = () => {

    // const navigate = useNavigate();
    return (
        <React.Fragment>
            Parent Messages
        </React.Fragment>
    );
};

export default ParentMessages;